---
import { Languages } from '~types/enum';
import Layout from '~layouts/Layout.astro';

import App from '~components/App';
interface Props {
  lang?: Languages;
}

const { lang = Languages.en } = Astro.props;
---

<Layout lang={lang}>
  <App lang={lang} client:load />
</Layout>
