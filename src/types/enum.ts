export enum Pages {
  home = 'home',
  share = 'share'
}

export enum Languages {
  en = 'en',
  fr = 'fr'
}

export enum Sections {
  Loading = 'Loading',
  Home = 'Home'
}

export enum SectionAnimStatus {
  None,
  In,
  InEnd,
  Out,
  OutEnd
}
