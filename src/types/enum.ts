export enum Pages {
  home = 'home',
  about = 'about',
  projects = 'projects',
  blog = 'blog'
}

export enum Languages {
  en = 'en',
  fr = 'fr'
}

export enum PageAnimStatus {
  None,
  In,
  InEnd,
  Out,
  OutEnd
}
