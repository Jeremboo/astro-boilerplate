export enum Languages {
  en = 'en',
  fr = 'fr'
}

export enum PageAnimStatus {
  In,
  InEnd,
  Out,
  OutEnd
}

export enum Scenes {
  Main = 'MainScene'
}

export enum AssetType {
  audio = 'audio',
  audioPositional = 'audio-positional',
  video = 'video',
  gltf = 'gltf',
  image = 'image',
  ktx2Texture = 'ktx2Texture',
  texture = 'texture'
}
